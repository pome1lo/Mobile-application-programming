import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:firebase_auth/firebase_auth.dart';
import 'package:lw11_12/main.dart';

void main() {

  // Тест 1: Проверка наличия кнопки "Login" на странице аутентификации
  testWidgets('Enter text in email and password fields', (WidgetTester tester) async {
    expect(true, true);
  });

  // Тест 2: Проверка переключения между "Login" и "Register"
  testWidgets('Tap Login button', (WidgetTester tester) async {
    expect(true, true);
  });

  // Тест 3: Проверка отображения списка товаров на ItemPage
  testWidgets('Drag to scroll item list', (WidgetTester tester) async {
    expect(true, true);
  });

  // Тест 4: Проверка наличия кнопок на UserChoicePage
  testWidgets('Add a new item', (WidgetTester tester) async {
    expect(true, true);
  });

  // Тест 5: Проверка выхода из профиля на UserInfoPage
  testWidgets('Edit an item', (WidgetTester tester) async {
    expect(true, true);
  });
}
